# fixtures/basic (placeholder)

This directory will become a small sample Stripe-integrating TypeScript repo used
as an end-to-end test fixture for Mendr.

**Status:** placeholder only. The full fixture (a minimal repo that calls the
Stripe SDK, with a known breaking change to fix) will be built in **Phase 7**.
