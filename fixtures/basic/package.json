{
  "name": "mendr-fixture-basic",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sample Stripe-integrating TypeScript repo used as an end-to-end test fixture for Mendr.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "tsc",
    "pretest": "tsc",
    "test": "node --test dist"
  },
  "dependencies": {
    "stripe": "^17.0.0"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
