node_modules
dist
.env

# fixture sample repos install/build their own artifacts
fixtures/**/node_modules
fixtures/**/dist

# throwaway prototyper spikes
spikes/

# mendr is not a consumer of itself — its own `watch` scan output stays untracked
.mendr/
