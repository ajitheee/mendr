import type { AffectedSite } from '../types.js';

// TODO Phase 5: apply a type-change fix at each affected site via ts-morph.

/** Generate edits that adapt to a type change at every affected site. */
export function fixTypeChange(site: AffectedSite): void {
  throw new Error('fixTypeChange not implemented yet (Phase 5)');
}
