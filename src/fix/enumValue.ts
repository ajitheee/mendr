import type { AffectedSite } from '../types.js';

// TODO Phase 6: apply an enum-value-change fix at each affected site via ts-morph.

/** Generate edits that update a changed enum value at every affected site. */
export function fixEnumValue(site: AffectedSite): void {
  throw new Error('fixEnumValue not implemented yet (Phase 6)');
}
