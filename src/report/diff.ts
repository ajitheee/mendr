import type { AffectedSite } from '../types.js';

// TODO Phase 6: render a unified diff of the applied fixes using the `diff` package.

/** Render a unified diff string describing the fixes for the affected sites. */
export function renderDiff(sites: AffectedSite[]): string {
  throw new Error('renderDiff not implemented yet (Phase 6)');
}
