# Release candidate — 2026-09-16

The artifact the first external customer installs. Everything below is recorded so a scan can be
reproduced exactly, and so nothing about it has to be taken on trust.

## Identity

| | |
|---|---|
| **Commit SHA** | `45181a997d02bbbe7659bcbd6b3e1a71e3cca0a9` |
| **Committed** | 2026-09-16T14:46:03-07:00 |
| **Package version** | `0.5.1-alpha` |
| **Artifact** | `mendr-0.5.1-alpha.tgz` — 95 files, 2,672,952 bytes unpacked |
| **Artifact sha256** | `bb12db2464dff49dcb12badc031cf301f14d666fe4c4aafbd98e3c15b6660cf7` |

A later docs-only commit adds this file. The shipped code is identical; pin whichever you prefer,
but pin a **40-character SHA**, not a tag. Tags are movable and a moved tag is indistinguishable
from the original.

## Registry

| | |
|---|---|
| **Version (content hash)** | `sha256:8241d681b80651cd` |
| **Full sha256** | `8241d681b80651cd4aa76828c173829dd7b5d08ec0d1a515d5f3ee11d48df3f6` |
| **Entries** | 158 (154 `model_id` records) |
| **Providers** | anthropic, google, openai |
| **Signed snapshot published** | 2026-09-16T19:02:24Z, from `818617d3c6bfad520cd16f2ab4dbc88ead4b7105` |
| **Signature** | `manifest.sig`, Ed25519, 89 bytes, at the rolling `registry-latest` release |
| **Public key** | `src/registry/trustedKeys.ts` (one trusted key) |
| **Rollback floor** | `2026-09-16T07:16:41Z` — a snapshot older than this is refused |
| **Max age** | 14 days; a stale registry makes a zero-finding result `inconclusive`, never clean |

## Validator

`mendr validate-registry` at this commit: **0 violations across 154 model_id records.** Checks
internal contradictions, including a replacement that is itself deprecated and a parameter rule
whose `on_models` list does not cover the replacement it ships with.

## Validation report

- `VALIDATION-2026-09-15.md` — twelve public repositories against the shipped artifact:
  15,712 analyzed source files, **one** automatically fixable finding, and it was correct.
  Zero parse failures, zero unopenable files.
- Its **2026-09-16 addendum** — records that the recall measurement had a blind spot, and why.
- **1,150 tests** at this commit.

## Supported surfaces

**Languages read:** TypeScript/TSX, JavaScript, Python. **Nothing else.** Go, Java, Kotlin, Rust,
C/C++, Ruby, PHP and C# are counted and reported as unread, never analyzed.

**Config read:** YAML and JSON/JSONC (block and flow style), TOML and `.env` located but not
parsed. Shell scripts are **not** read — a model id set in a `.sh` file and passed as a CLI
argument is invisible.

**Providers:** anthropic, google, openai.

**Tiers:** A = auto-patchable (requires a first-party SDK call with the client resolvable in the
same file). B = review required. C = informational.

---

# Known limitations

Read this section before onboarding anyone.

## 1. A registry change can publish a false "resolved" and close the issue — CONFIRMED, UNFIXED

The highest-severity known defect. Independently reproduced through the real rendering path.

A finding exists only where a scanned occurrence **joins** a registry entry. The generated audit
workflow sets `MENDR_REGISTRY_REFRESH: 'on'` and fetches the **rolling** snapshot, so the registry
data moves between runs while the pinned code does not — by design (`freshRegistry.ts`: "pin the
CODE, refresh the DATA").

So: run 1 raises a finding. A later registry publish re-keys, re-statuses or drops that entry.
Run 2 — same commit, same repo, same files, every surface completing — finds nothing. The existing
guard (`resolutionsAreTrustworthy`) only asks whether a surface **errored**; nothing errored. The
finding is reported **resolved**, `mayClose` returns true, and the issue is closed with the
baseline erased.

`coverage.registry.version` is already a content hash and is already in scope at the diff site.
It is simply not consulted. The fix is to make the resolution identity `(scanner, registry)`
rather than scanner alone — deferred under the freeze, recorded here.

**Operationally, until fixed:** do not treat a closed audit issue as proof a finding was fixed.
Confirm against the merged PR.

## 2. Tier A almost never fires on real code

Measured across the 12-repo validation corpus plus 14 outreach repositories: **one** Tier A
finding in 26 real repositories. Tier A requires the SDK client to resolve in the same file, and
production code injects or wraps its client. Expect a **report**, not a patch. Any pitch built on
"it opens the PR for you" will not survive contact with a real repository.

## 3. The verification environment is not isolated

It is a **secret-sanitized verification environment**: a throwaway copy with the CI's own
credentials stripped and captured output redacted at source. There is **no** network isolation, no
process or filesystem isolation, and no resource limits. Running a gate executes the repository's
own code with its own privileges. Full statement in `TRUST.md` §4b.

## 4. Behaviour is never verified

The gates prove a change builds and the existing tests pass. They prove nothing about whether the
replacement model matches the old one on quality, latency, cost or response shape. Every PR body
says so.

## 5. The recall measurement has a known blind spot

The 12-repo recall search narrowed occurrences to candidates using the scanner's own notion of a
candidate. It can catch an id sorted into the wrong bucket; it structurally **cannot** catch a
whole position the classifier misreads. That class was found by reading a repository by hand
(`going-doer/Paper2Code`), which produced v0.5.1-alpha.

## 6. Distribution is by git spec, not npm

`npx --yes "github:ajitheee/mendr#<sha>"`. The npm package name is unclaimed; `npx mendr` does
not work.

## 7. The approval loop is unexercised on this build

`mendr-demo` has two merged Mendr PRs, but both are from 2026-09-10, three releases back. The
Approve-to-PR path has **not** been run end to end on this artifact.

## 8. Inherited uncertainty

Config selection other than an env-var read is not traced to a runtime reader ("reader tie-back
not proven"). Runtime usage is unmeasured unless telemetry is connected, so "is this live in
production" is unanswered by default.

---

## How the customer pins this

```yaml
uses: ajitheee/mendr/.github/workflows/reusable-audit.yml@45181a997d02bbbe7659bcbd6b3e1a71e3cca0a9
uses: ajitheee/mendr/.github/workflows/reusable-migrate.yml@45181a997d02bbbe7659bcbd6b3e1a71e3cca0a9
```

Set the repository variable `MENDR_SPEC` to the same SHA so the CLI it fetches is pinned too.
